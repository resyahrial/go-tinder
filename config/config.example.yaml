app:
  rest:
    enabled: true
    name: gotinder
    port: 8080
store:
  postgresql:
    name: gotinder_db
    user: postgres
    password: postgres
    host: localhost
    port: "5432"
  migration:
    tablename: gotinder_migrations
  redis:
    database: 1
    password: redis
    host: localhost
    port: 6379
